stages :
- test

test:
    stage: test
    before_script:
    - chcp 65001
    - pip install --user -r requirements.txt
    script:
    - echo "Start testing"
    - cd tests
    - python -m pytest tests.py -v
    - echo "End testing"
    - cd ..
